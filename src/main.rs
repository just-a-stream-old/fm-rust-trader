mod config;
mod model;
mod data;

use crate::config::*;
use crate::model::SymbolData;
use std::intrinsics::fabsf32;

fn main() {
    // Load environment configuration
    let config = load_config();


}
